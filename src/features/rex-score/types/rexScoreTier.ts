export type RexScoreTier = {
  code: string;
  label: string;
  icon: string;
  sort_order: number;
  badge_background: string;
  text_color: string;
  border_color: string;
  glow_color: string;
  description: string | null;
  next_tier_hint: string | null;
};
