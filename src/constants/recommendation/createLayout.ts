/** Max width for step body + footer inside the modal card */
export const CREATE_REC_MODAL_MAX_W = 528;

/** Tailwind classes: centered column, max width, step padding */
export const CREATE_REC_STEP_INNER = 'w-full max-w-[528px] mx-auto p-6';
